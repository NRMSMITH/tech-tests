//backToFront is a function which takes an array, returns a new array and in the process, takes all of the contents of the original array, passes it to the new array and puts the last item of the array at the starting index of the new array.

//The tests are written for you, you must use TDD.

const backToFront = () => {
}

module.exports = backToFront;